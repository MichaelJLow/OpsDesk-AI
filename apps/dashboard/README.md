# OpsDesk Dashboard

Next.js operator UI for OpsDesk AI.

## Scripts

```powershell
cd apps/dashboard
npm install
npm run dev
```

- App: http://localhost:3000
- Health: http://localhost:3000/api/health

Do not build the full inbox UI until the Phase 3 sales vertical slice works.
